/**
 * Ambient types for the repo.
 *
 * `bun` brings in the `bun:test` module declaration used by `*.test.ts`
 * (tests run on `bun test`, not on a bundled runner).
 */
/// <reference types="bun" />
